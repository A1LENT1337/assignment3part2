# Habit Tracker (Vetain-style) — Node.js + Express + MongoDB (Native Driver)

Academic project: Habit Tracker web application using **Express** and **MongoDB Native Node.js Driver (MongoClient)** only.  
No Mongoose is used.

## Setup

1) Install dependencies:
```bash
npm install
